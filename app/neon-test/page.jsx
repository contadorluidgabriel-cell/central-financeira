import NeonTestPanel from '../../components/NeonTestPanel';

export const metadata = {
  title: 'Neon Test Lab — Central Financeira',
  description: 'Ambiente isolado para validar Neon Auth, Data API e RLS.'
};

export default function NeonTestPage() {
  return <NeonTestPanel />;
}
