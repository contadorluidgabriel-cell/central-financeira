function dashboardHtml(){const rows=allRows(),total=rows.length,waiting=rows.filter(x=>x.s[2]==='waiting'||x.s[2]==='filling').length,analysis=rows.filter(x=>x.s[2]==='ready'||x.s[2]==='analysis').length,closed=rows.filter(x=>x.s[2]==='closed').length,rev=rows.reduce((a,b)=>a+b.rev,0);return `<div class="pagehead"><div><h1>Visão geral</h1><p>Carteira de ${monthName(state.month).toLowerCase()} e o andamento da competência.</p></div><div class="actions"><button class="btn btn-secondary" onclick="setView('companies')">Ver clientes</button><button class="btn btn-primary" onclick="openCompanyModal()">${I('plus')} Novo cliente</button></div></div><div class="grid kpis">${kpi('users','Clientes ativos',total,'Empresas cadastradas')}${kpi('alert','Aguardando cliente',waiting,waiting?'Dados ou confirmação pendentes':'Carteira em dia')}${kpi('chart','Em andamento',analysis,'Prontos ou em análise')}${kpi('money','Receitas acompanhadas',BRL(rev),'Somatório da competência')}</div><div class="grid dashboardgrid"><section class="panel"><div class="panelhead"><div><h3>Carteira</h3><p>Abra um cliente para trabalhar na competência.</p></div><button class="btn btn-secondary" onclick="setView('companies')">Ver todos</button></div><div class="tablewrap">${companyTable(rows.slice(0,6))}</div></section><section class="panel"><div class="panelhead"><div><h3>Fluxo do mês</h3><p>${closed} de ${total} clientes fechados</p></div></div><div class="panelbody"><div class="progresslist">${prog('Aguardando cliente',waiting,total)}${prog('Em andamento',analysis,total)}${prog('Fechados',closed,total)}</div><div class="divider"></div>${waiting?`<div class="signal"><div class="signalicon warn">${I('alert')}</div><div><strong>${waiting} cliente(s) com envio pendente</strong><p>Abra a carteira para ver exatamente o que falta em cada empresa.</p></div></div>`:`<div class="signal"><div class="signalicon good">${I('check')}</div><div><strong>Todos concluíram o envio</strong><p>A carteira já pode avançar para análise e fechamento.</p></div></div>`}</div></section></div>`}
function kpi(i,l,v,f){return `<div class="card kpi"><div class="kpitop"><span class="kpilabel">${l}</span><div class="kpiicon">${I(i)}</div></div><div class="kpivalue">${v}</div><div class="kpifoot">${f}</div></div>`}function prog(l,n,t){const p=t?Math.round(n/t*100):0;return `<div class="progressrow"><span>${l}</span><b>${n}</b><div class="track"><div class="bar" style="width:${p}%"></div></div></div>`}
function companyTable(rows){if(!rows.length)return `<div class="empty"><div class="iconwrap">${I('search')}</div><strong>Nenhum cliente encontrado</strong><p>Ajuste a busca ou os filtros.</p></div>`;return `<table><thead><tr><th>Cliente</th><th>Receitas</th><th>Saldo</th><th>Status</th><th>Pendência</th><th></th></tr></thead><tbody>${rows.map(c=>`<tr class="clickable" onclick="openCompany('${c.id}')"><td><div class="companycell"><div class="avatar">${initials(c.name)}</div><div><strong>${esc(c.name)}</strong><span>${esc(c.document||'Sem documento')}</span></div></div></td><td>${BRL(c.rev)}</td><td>${c.expenseEnabled?`<b class="${c.balance>=0?'positive':'negative'}">${BRL(c.balance)}</b>`:'<span class="muted">Não acompanhado</span>'}</td><td><span class="status ${c.s[1]}">${c.s[0]}</span></td><td><span class="pendingtext ${c.s[2]==='waiting'||c.s[2]==='filling'?'warntext':''}">${esc(pendingText(c))}</span></td><td>${I('arrow')}</td></tr>`).join('')}</tbody></table>`}
function companiesHtml(){let rows=allRows();const q=(state.search||'').toLowerCase().trim();if(q)rows=rows.filter(c=>(c.name+' '+c.document+' '+c.contact).toLowerCase().includes(q));if(state.filter!=='all')rows=rows.filter(c=>c.s[2]===state.filter||(state.filter==='progress'&&(c.s[2]==='ready'||c.s[2]==='analysis'))||(state.filter==='waiting'&&(c.s[2]==='waiting'||c.s[2]==='filling')));return `<div class="pagehead"><div><h1>Clientes</h1><p>Carteira, configuração e andamento mensal em um único lugar.</p></div><div class="actions"><button class="btn btn-primary" onclick="openCompanyModal()">${I('plus')} Novo cliente</button></div></div><div class="filters" style="margin-bottom:14px"><div class="search">${I('search')}<input class="input" placeholder="Buscar cliente, CNPJ ou e-mail" value="${esc(state.search||'')}" oninput="state.search=this.value;save();render()"></div><div class="chips">${[['all','Todos'],['waiting','Aguardando'],['progress','Em andamento'],['closed','Fechados']].map(x=>`<button class="chip ${state.filter===x[0]?'active':''}" onclick="state.filter='${x[0]}';save();render()">${x[1]}</button>`).join('')}</div></div><section class="panel"><div class="panelhead"><div><h3>${rows.length} empresa(s)</h3><p>Competência: ${monthName(state.month)}</p></div></div><div class="tablewrap">${companyTable(rows)}</div></section>`}
function closingsHtml(){const rows=allRows();return `<div class="pagehead"><div><h1>Fechamentos</h1><p>O cliente conclui o envio antes da análise, e o mês fechado fica bloqueado.</p></div></div><section class="panel"><div class="tablewrap"><table><thead><tr><th>Cliente</th><th>Modelo</th><th>Receitas</th><th>Despesas</th><th>Status</th><th>Pendência</th><th></th></tr></thead><tbody>${rows.map(c=>`<tr class="clickable" onclick="openCompany('${c.id}','closing')"><td><div class="companycell"><div class="avatar">${initials(c.name)}</div><div><strong>${esc(c.name)}</strong><span>${esc(c.contact)}</span></div></div></td><td>${c.expenseEnabled?'Receitas + despesas':'Somente receitas'}</td><td>${BRL(c.rev)}</td><td>${c.expenseEnabled?BRL(c.exp):'—'}</td><td><span class="status ${c.s[1]}">${c.s[0]}</span></td><td><span class="pendingtext">${esc(pendingText(c))}</span></td><td>${I('arrow')}</td></tr>`).join('')}</tbody></table></div></section>`}
function settingsHtml(){return `<div class="pagehead"><div><h1>Configurações</h1><p>Parâmetros gerais do protótipo.</p></div></div><div class="grid sectiongrid"><section class="panel span7"><div class="panelhead"><div><h3>Escritório</h3><p>Identidade do usuário Master.</p></div></div><div class="panelbody"><div class="formgrid"><div class="field full"><label>Nome</label><input class="input" value="Contador Luid Gabriel"></div><div class="field"><label>Usuário Master</label><input class="input" value="Luid Gabriel"></div><div class="field"><label>Perfil</label><input class="input" value="Super Admin" disabled></div></div><button class="btn btn-primary" onclick="toast('Configurações salvas na demonstração')">Salvar</button></div></section><section class="panel span5"><div class="panelhead"><div><h3>Dados da demonstração</h3><p>Ferramentas de teste.</p></div></div><div class="panelbody"><div class="signal"><div class="signalicon info">${I('lock')}</div><div><strong>Ainda sem Supabase</strong><p>Os dados ficam apenas no navegador nesta etapa.</p></div></div><button class="btn btn-danger btn-block" onclick="resetDemo()">Restaurar dados da V1.1</button></div></section></div>`}
